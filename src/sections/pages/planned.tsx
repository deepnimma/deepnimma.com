import '../../css/pages/Planned.css'
import React from "react"

const Planned: React.FC = () => {
    return (
        <div className={"plannedCon"}>
            <span className={"plannedText"}>Planned Changes to the Website.</span>
        </div>
    ) // return
} // Planned

export default Planned